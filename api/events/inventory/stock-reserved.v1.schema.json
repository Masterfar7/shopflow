{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://shopflow.io/schemas/events/inventory/stock-reserved.v1.json",
  "title": "StockReservedPayloadV1",
  "type": "object",
  "required": [
    "reservation_id",
    "order_id",
    "items",
    "expires_at"
  ],
  "properties": {
    "reservation_id": { "type": "string", "format": "uuid" },
    "order_id": { "type": "string", "format": "uuid" },
    "items": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["sku", "quantity"],
        "properties": {
          "sku": { "type": "string" },
          "quantity": { "type": "integer", "minimum": 1 }
        }
      }
    },
    "expires_at": { "type": "string", "format": "date-time" }
  }
}
