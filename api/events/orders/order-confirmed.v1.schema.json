{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://shopflow.io/schemas/events/orders/order-confirmed.v1.json",
  "title": "OrderConfirmedPayloadV1",
  "type": "object",
  "required": [
    "order_id",
    "customer_email",
    "total_amount_minor",
    "currency",
    "line_items",
    "confirmed_at"
  ],
  "properties": {
    "order_id": { "type": "string", "format": "uuid" },
    "customer_id": { "type": "string" },
    "customer_email": { "type": "string", "format": "email" },
    "total_amount_minor": { "type": "integer", "minimum": 0 },
    "currency": { "type": "string", "pattern": "^[A-Z]{3}$" },
    "line_items": {
      "type": "array",
      "minItems": 0,
      "items": {
        "type": "object",
        "required": ["sku", "title", "unit_price_minor", "quantity", "subtotal_minor"],
        "properties": {
          "sku": { "type": "string" },
          "title": { "type": "string" },
          "unit_price_minor": { "type": "integer", "minimum": 0 },
          "quantity": { "type": "integer", "minimum": 1 },
          "subtotal_minor": { "type": "integer", "minimum": 0 }
        }
      }
    },
    "confirmed_at": { "type": "string", "format": "date-time" }
  }
}
