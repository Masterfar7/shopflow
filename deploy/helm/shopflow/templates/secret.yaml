apiVersion: v1
kind: Secret
metadata:
  name: {{ include "shopflow.fullname" . }}-secret
  labels:
    {{- include "shopflow.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $val := .Values.secrets }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
