apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "shopflow.fullname" . }}-config
  labels:
    {{- include "shopflow.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
